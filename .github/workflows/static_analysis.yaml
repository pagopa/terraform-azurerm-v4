name: Static Analysis

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  static_analysis:
    runs-on: ubuntu-22.04

    steps:
        # from https://github.com/actions/checkout/commits/main
      - uses: actions/checkout@1f9a0c22da41e6ebfa534300ef656657ea2c6707

      - name: run_init
        run: |
          bash .utils/terraform_run_all.sh init docker

      - name: show_version
        run: |
          TAG="v1.105.0sha256:4ef4b8323b27fc263535ad88c9d2f20488fcb3b520258e5e7f0553ed5f6692b5"
          docker run --rm --entrypoint cat ghcr.io/antonbabenko/pre-commit-terraform:$TAG /usr/bin/tools_versions_info

      - name: run_test
        run: |
          TAG="v1.105.0sha256:4ef4b8323b27fc263535ad88c9d2f20488fcb3b520258e5e7f0553ed5f6692b5"
          docker run -v $(pwd):/lint -w /lint ghcr.io/antonbabenko/pre-commit-terraform:$TAG run -a
